"""VIZ — court plots, overlay renderer, reliability diagrams, timelines.

All figures use the non-interactive Agg backend so they render headless and are
regenerable from a committed config + seed (plan gate G5).
"""
